package model

import "testing"

func TestDeckList(t *testing.T) {
	tests := []struct {
		name     string
		expected []Deck
		err      string
	}{}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {

		})
	}
}
